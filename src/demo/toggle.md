 # cols[6]
 ```html
<ld-toggle-buttons
  v-model="value"
  :items="items"
  :disabled="disabled"
  :rounded="rounded"
  :size="size"
/>
```
 # end of cols
 # cols[6]
 * Props

 ```js
modelValue: number | string // модель
items: Array<{
  id: number;
  tooltip: string;
  icon: string;
  text: string;
}>
size: string // s (default), m, l
rounded: boolean // (default: false), закругленные углы и более квадратные
disabled: boolean //
```
 # end of cols

